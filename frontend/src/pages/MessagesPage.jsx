export default function MessagesPage() {
  return (
    <main>
      <h1>Direct Messages</h1>
      <p>One-on-one real-time chat UI should consume /api/messages and Socket.io events.</p>
    </main>
  );
}
